name: flutter_wearos_location
description: "A Test project for wearOS Location interaction"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ^3.3.0
  flutter: ^3.19.2

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^11.0.0
  #  location: ^5.0.3
  hooks_riverpod: ^2.5.1
  jni: ^0.8.0
  jnigen: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
